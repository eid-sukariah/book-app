<!DOCTYPE html>
<html lang="en">
  <%- include('../../layout/head') %>
  <body>
   
    <main>
      <%- include('../../layout/header') %>

        <% searchResults.forEach( book => { %>

          <%- include('../books/detail', {book}) %>

        <% }) %>
        <form action="/searches" method="post">
          <input class="search" type="text" name="search" placeholder="Enter a title or author" required>
          <fieldset class="check">
            <label for="title">
              <span>Title</span>
              <input type="radio" name="search" value="title" required>
            </label>
          </fieldset>
          <fieldset class="check">
            <label for="author">
              <span>Author</span>
              <input type="radio" name="search" value="author" required>
            </label>
          </fieldset>
          <button type="submit">Search the Google Books API</button>
        </form>
    </main>
    <%- include('../../layout/footer') %>
  </body>
</html>